server:
  port: 8761

spring:
  application:
    name: discovery-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

# ⛔ Prevents Eureka client auto-registration from being created
spring.cloud:
  service-registry:
    auto-registration:
      enabled: false
